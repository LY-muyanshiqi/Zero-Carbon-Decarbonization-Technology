# 进行中

> 更新：2026-08-21

## 当前任务

**修复报告口径**：清理编造的 LightGBM/BiLSTM 内容，改回真实的 Holt-Winters + KNN 方案。（今日会话）

## 本次会话完成（2026-08-21）

### 一、发现严重问题：报告里有编造的 LightGBM

- 排查发现 `模块一_负荷预测_技术方案报告.docx` 和生成脚本 `gen_module1_report.mjs` 里**编造了不存在的 LightGBM 模型和假数据**（MAPE 4.22%、R² 0.933、MAE 0.81 kW、10,944 条、假 csv 清单、假"7月15日预测算例"）
- 真实算法是 **Holt-Winters + KNN**（代码 `负荷预测_v2.py` 可跑，`import lightgbm` 全仓库 0 处）
- 真实精度：A 公司日总 MAPE 9.08%、逐时 MAE 0.46 MW（V2，进行中.md 原有口径）

### 二、已改文件（3 个）

| 文件 | 改动 |
|------|------|
| `gen_module1_report.mjs` | 删 4.3 LightGBM/4.4 BiLSTM/假数据，改写为真实 HW+KNN |
| `plot_four_charts.py` | 图4 三模型真实对比（相似日基线/XGB旧法/HW+KNN新法） |
| `模块一_负荷预测_技术方案报告.docx` | python-docx 逐段替换 14 处编造段落，**保留 17 张图** |

### 三、Git 同步（待确认后执行）

- 本次改动 3 个文件 + 之前内容都还在本地，**尚未 commit/push**
- 等口径确认后统一推送

## 待做（重要）

1. **⚠️ 报告口径未完全统一**：报告里仍有 agent 单元不一致
   - 负荷：报告写"24.2 kW""580 kWh" vs 进行中.md 写"MW 平均功率"
   - 光伏：报告封面写"8MWp" vs 进行中.md 写"12.8MW（6.4MW×2放大）"
   - 储能：报告写"0.22MWh" vs 进行中.md 写"4MWh/2MW"
   - 4.2 章节"A 公司逐时 MAE = 0.46 **kW**" 应为 **0.46 MW**
2. **LightGBM/BiLSTM 原理图孤儿文件**（`diagram/4.3_*`、`4.4_*`、`lgbm_*`、`plot_lightgbm_principle.py`）用户决定"暂时留着"，后续统一清理
3. 申报书：还没开始改（用户提到"都开始写申报书了"——申报书是另写的新文档，需单独对齐口径）

## 关键背景

- 用户（雅妮）通过让 AI 去 GitHub 搜，发现项目里没有 LightGBM，担心申报书对不上
- Claude 排查确认：LightGBM 从头到尾不存在，是某次写报告时编造的
- 真实方案：**Holt-Winters（日总）+ KNN（逐时分配）+ 7因子相似日 + 残差守门**，有真实回测数据（A 公司 MAPE 9.08% / MAE 0.46 MW）
- 冲突点：这里存在口径差异（9.08% vs 设计文档 13.80%），需要确认以哪个为准

## 待做

- 光伏补全到 396 天（与负荷对齐，目前光伏只到 365 天）
- 前端启动本地验证（Streamlit 实际跑一遍看效果）
- 最终成果汇总表更新（含前端改动后的最新数字）
- 答辩口径文档（模块二答辩口径已有，需补模块三 + 前端整体叙事）
